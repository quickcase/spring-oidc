plugins {
    id 'io.freefair.lombok' version '8.13'
}

dependencies {
    api project(':api')
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            artifactId = 'oidc'
        }
    }
}
