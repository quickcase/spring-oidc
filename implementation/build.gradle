plugins {
    id 'io.freefair.lombok' version '8.12.2.1'
}

dependencies {
    api project(':api')
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            artifactId = 'oidc'
        }
    }
}
