plugins {
    id 'io.freefair.lombok' version '8.10.2'
}

dependencies {
    api project(':api')
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            artifactId = 'oidc'
        }
    }
}
