plugins {
    id 'io.freefair.lombok' version '6.4.1'
}

dependencies {
    api project(':api')
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            artifactId = 'oidc'
        }
    }
}
