plugins {
    id 'io.freefair.lombok' version '5.3.0'
}

repositories {
    jcenter()
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            artifactId = 'oidc-api'
        }
    }
}
